# AGENTS.md — Astera Online

**The operating manual is [`CLAUDE.md`](CLAUDE.md). Read that.**

This file exists only because some tools look for `AGENTS.md` by convention. It deliberately
holds no content of its own: it was a copy of `CLAUDE.md` once, the copy went stale, and two
files claiming to be the manual is worse than one.
